---
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';

import { Background } from '@/components/background';
import { FAQ } from '@/components/blocks/faq';
// import { Testimonials } from '@/components/blocks/testimonials'; // TODO: re-enable when testimonials/case studies are ready
// import { DashedLine } from '@/components/dashed-line'; // TODO: re-enable with testimonials
import DefaultLayout from '@/layouts/DefaultLayout.astro';
---
<DefaultLayout title={SITE_TITLE} description={SITE_DESCRIPTION}>
  <Background>
    <FAQ
      client:only='react'
      className="py-28 text-center lg:pt-44 lg:pb-32"
      className2="max-w-xl lg:grid-cols-1"
      headerTag="h1"
    />
    <!-- <DashedLine className="mx-auto max-w-xl" /> -->
    <!-- <Testimonials dashedLineClassName="hidden" client:only='react' /> --> <!-- TODO: re-enable when testimonials/case studies are ready -->
  </Background>
</DefaultLayout>
